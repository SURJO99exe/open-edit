// Terminal service for terminal operations
export const terminalService = {
  // Terminal operations will be implemented later
  execute: async (command: string) => {
    return { output: '', error: null };
  }
};